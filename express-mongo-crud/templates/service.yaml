apiVersion: v1
kind: Service
metadata:
  name: {{ template "express-mongo-crud.fullname" . }}
  labels:
    app: {{ template "express-mongo-crud.name" . }}
    chart: {{ template "express-mongo-crud.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "express-mongo-crud.name" . }}
    release: {{ .Release.Name }}
